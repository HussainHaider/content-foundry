# LangGraph + LangChain (1.x ecosystem — pinned exactly; see requirements.lock for full freeze)
langgraph==1.2.6
langchain==1.3.11
langchain-openai==1.3.3
langchain-anthropic==1.4.8
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-qdrant==1.1.0
langchain-voyageai==0.4.0

# LLM
openai==2.44.0
anthropic>=0.34.0

# Vector DB
qdrant-client>=1.11.0

# Document parsing for RAG ingestion
pymupdf>=1.24.0
pypdf>=4.0.0

# Web search (Serper, via langchain-community's GoogleSerperAPIWrapper)
requests>=2.32.0

# UI
streamlit>=1.39.0
pandas>=2.0.0

# Publishing APIs
notion-client>=2.2.3
httpx>=0.27.0

# Storyblok publishing service (FastAPI + markdown parsing)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
markdown-it-py>=3.0.0

# Observability
langsmith>=0.1.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Utilities
python-dotenv>=1.0.0
